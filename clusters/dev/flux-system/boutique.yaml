apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: boutique
  namespace: flux-system

spec:
  interval: 5m

  sourceRef:
    kind: GitRepository
    name: flux-system

  path: ./

  prune: true
  wait: true
